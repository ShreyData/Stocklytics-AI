steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - backend/Dockerfile
      - -t
      - ${_AR_HOSTNAME}/${PROJECT_ID}/${_AR_REPO}/stocklytics-backend:${_IMAGE_TAG}
      - backend

images:
  - ${_AR_HOSTNAME}/${PROJECT_ID}/${_AR_REPO}/stocklytics-backend:${_IMAGE_TAG}
